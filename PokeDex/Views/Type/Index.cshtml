@model List<Pokedex.Application.ViewModel.TypeViewModel>

@{
    ViewData["Title"] = "Type List";
}

<div class="row">
    <div class="col-6 offset-3">
        <table class="table table-striped table-hover table-type border border-light caption-top">
             <caption><h2>Type List</h2></caption>
            <thead class="table-type-head">
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th> </th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {
                 <tr>
                     <td>@item.TypeId</td>
                     <td>@item.Name</td>
                     <td>
                     <a asp-action="Edit" asp-controller="Type" asp-route-id="@item.TypeId" class="btn btn-warning">Edit</a>
                     <a asp-action="Delete" asp-controller="Type" asp-route-id="@item.TypeId" class="btn btn-danger">Delete</a>
                     </td>
                 </tr>   
                }
            </tbody>
        </table>
    </div>
</div>


