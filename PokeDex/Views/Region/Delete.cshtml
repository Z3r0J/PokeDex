@model Pokedex.Application.ViewModel.AddRegionViewModel

@{
    ViewData["Title"] = "Delete Region";
}

<div class="row">
    <div class="col-md-3 offset-4">
        <div class="card">
            <div class="card-header badge-yellow text-center">
                <h3>Delete Region</h3>
            </div>
            <div class="card-body">
                <h5 class="text-center">¿Are you sure want to delete @Model.Name?</h5>
                <form asp-controller="Region" asp-action="DeletePost" method="post">
                    <input asp-for="Id" type="hidden" />
                    <button type="submit" class="btn btn-outline-success">Delete</button>
                    <a asp-action="Index" class="btn btn-outline-danger">Cancel</a>
                </form>
            </div>
        </div>
</div>
</div>

